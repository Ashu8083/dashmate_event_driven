server:
  port : 8083
spring :
  datasource :
    url : jdbc:postgresql://localhost:5432/store
    username : postgres
    password : root
  jpa :
    hibernate :
      ddl-auto : none
  flyway :
    enabled : true
    location : classpath:db/migration
logging:
    level:
      org.flywaydb: DEBUG