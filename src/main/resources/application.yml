spring :
  datasource :
    url : jdbc:postgresql://localhost:5432/store
    username : postgres
    password : root
  jpa :
    hibernate :
      ddl-auto : none
  flyway :
    enabled : true
logging:
    level:
      org.flywaydb: DEBUG